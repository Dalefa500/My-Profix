# Caddy сам получает и продлевает бесплатный SSL-сертификат
# для домена из APP_DOMAIN — настраивать сертификаты вручную не нужно.
# В APP_DOMAIN можно перечислить несколько адресов через запятую: на
# время переезда на новый домен старый продолжит работать.

{$APP_DOMAIN} {
	encode gzip
	reverse_proxy web:8000
}
